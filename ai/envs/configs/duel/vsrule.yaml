game:
  map_config: cn
  num_players: 2
  max_players: 6
  max_turns: 150
  capital_mode: fixed
  capitals: [8, 25]        # 黑龙江(东北) vs 云南(西南)

reward:
  win: 100.0
  lose: -100.0
  shaped:
    territory_gain: 1.0
    territory_loss: -1.0
    capital_capture: 20.0
    step_penalty: -0.5

observation:
  max_troops: 500
  max_growth: 10
  cmd_max: 16
  use_adjacency: true       # 邻接信息进观测（is_adj_to_my_territory，1 bit/region）

action:
  troop_buckets: [0.25, 0.5, 0.75, 1.0]

training:
  opponent: rule
