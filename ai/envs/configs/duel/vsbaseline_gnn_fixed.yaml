game:
  map_config: cn
  num_players: 2
  max_players: 6
  max_turns: 150
  capital_mode: fixed
  capitals: [4, 20]        # 河北(4) vs 江西(20)

reward:
  win: 100.0
  lose: -100.0
  shaped:
    territory_gain: 1.0
    territory_loss: -1.0
    capital_capture: 20.0
    step_penalty: -0.2

observation:
  max_troops: 500
  max_growth: 10
  cmd_max: 16
  use_adjacency: false      # GNN 自带拓扑感知

action:
  troop_buckets: [0.25, 0.5, 0.75, 1.0]

training:
  mode: vsbaseline
  opponent: random
